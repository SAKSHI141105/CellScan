# CPU wheels by default — swap for the CUDA index if training on GPU:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.2
torchvision>=0.17
timm>=0.9.16

pydicom>=2.4          # VOI LUT + MONOCHROME1/2 handling for raw mammography DICOMs
opencv-python-headless>=4.8
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3
albumentations>=1.4
PyYAML>=6.0
tqdm>=4.66
matplotlib>=3.7

pytest>=7.4
